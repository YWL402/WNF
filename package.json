{
  "name": "salary-calculator",
  "version": "1.0.0",
  "description": "实时计算收入的桌面组件",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:exe": "nexe main.js -o dist/窝囊废Desk.exe --build"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "electron": "^28.1.0",
    "nexe": "^4.0.0"
  },
  "dependencies": {
    "electron-store": "^10.0.1"
  }
}
